// @flow
// import type { Store } from './../types'

const initialState = {
  stuff: [],
  colors: ['red', 'yellow', 'blue'],
  formData: {
    mfi: null,
    loanType: null,
    productType: null,
    versionNum: null
  }
}

export default initialState
